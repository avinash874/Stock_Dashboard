# Local dev: leave empty — Vite proxies /companies, /data, … to localhost:8000.
# Production (Vercel): see `.env.production` (Render API) or set the same var in Vercel’s dashboard.

VITE_API_BASE=

# Optional: public URL of this frontend (footer “Live app” link). Default is the JarNox Vercel deploy.
