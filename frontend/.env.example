# Leave empty when the UI is served by FastAPI (same origin).
# On Vercel (or any static host), point this at your FastAPI server:
# VITE_API_BASE=https://your-api.example.com

VITE_API_BASE=

# Optional: public URL of this frontend (footer “Live app” link). Default is the JarNox Vercel deploy.
# VITE_APP_LIVE_URL=https://stock-dashboard-tawny-nine.vercel.app
